import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.barkoder.appcapacitor',
  appName: 'barkoder_app_capacitor',
  webDir: 'dist'
};

export default config;
